# GridWorld — project rules

Browser FPS in a green vector-grid style (wireframe on black, retro sci-fi / Tron / CRT terminal).
`legacy/grid-arena.html` is the original single-file version (from when the game was called Grid Arena), kept as the reference for behaviour.

## Commands
- `npm run dev` — dev server (Vite)
- `npm test` — Vitest (generators, terrain, saves)
- `npm run typecheck` / `npm run build`
- `npm run sim` — headless simulations in `tools/` (`tools/sim.config.ts`, files `*.sim.ts`): `tools/growth.sim.ts` runs every village of a world through village life (`SIM_WORLD`, `SIM_DAYS`, `SIM_EVERY` env vars) and prints population, food, gold, buildings, raids and the food price; `tools/civ.sim.ts` runs the civilisation layer (`SIM_PLAYERS=0,4`); `tools/campaign.sim.ts` walks a player bot through the campaign, times it and calibrates the Chariot's size to 80 h alone (`PACE` = the assumptions about gold per hour, speeds, dive times).
- Dev console: ~ (Backquote) opens it in every build (`ui/console.ts`): `cash` (+10000 gold), `god` (full health + immortality, toggle), `home` (back to Gridholm), `time [hour]` (show the clock / skip ahead), `worldmap` (whole-planet dev map, `ui/devmap.ts`: pan, zoom, click a place or spot to teleport via `teleportTo` in `world/level.ts`), `tp <x> <z>`, `fly [speed]` (dev flight: `G.fly`, `flyStep` in `world/player.ts`: no gravity or collisions, W/S along the view, Space/C up/down, Shift 3x; twice the load radius, fog pushed back, horizon rings hidden, `camera.far` 600, `mayspawn` refuses), `help`, `clear`. Add commands to `COMMANDS` there.
- In a dev build, `window.__game` exposes `{ G, W, camera, scene, renderer }` for debugging; F3 toggles the perf overlay (fps, lines, triangles, draw calls).

## Rules
- **After every finished task** (committed and pushed), end the reply with the download link of the current version: `https://github.com/MrFischoeder/Gridworld/archive/refs/heads/<branch>.zip` (the branch the work was pushed to). Unzip, `npm install`, then `start-gry.bat` or `npm run dev`.
- **Version number:** with every pushed change bump `VERSION` in `src/version.ts` (and `version` in `package.json` to match): the middle number for a new feature (0.5.0 → 0.6.0), the last one for a fix (0.6.0 → 0.6.1); also set `BUILD` (date · what changed) and add an entry at the top of `CHANGELOG` in `src/data/changelog.ts` (version, date, title, player-facing notes in English; shown by the Changelog button in the main menu, `ui/changelog.ts`) — every change, feature or fix, gets described there. The number is shown next to the title in the main menu ("GRIDWORLD v0.5.0") and in the browser tab; say the new number in the reply.
- **Stack:** Vite + TypeScript (strict), three.js from npm. No UI framework: HUD and windows are plain DOM + CSS.
- **Game language: English.** Every player-visible text (HUD, menus, dialogue, items, hints) is English. Identifiers and comments in English.
- **Deterministic generation:** the whole world (dungeons, villages, terrain, ruins, object placement) comes only from seeded RNG (`core/rng.ts`: `rng`, `hash`, noise). Generators **never** call `Math.random()`. `Math.random()` is allowed only for effects and unsaved things (sparks, drone loot, NPC wandering). Same seed + same coordinates = identical result. This is the basis of saves and future multiplayer.
- **Save only player changes** (opened chests, killed bosses, unlocked doors, position, character) — never generated geometry.
- **Visual style:** grid lines `#2fe060`, black background, fog to black (on the surface by day the sky and fog turn a bright hazy green, see the game clock note). Every solid that should hide what is behind it needs a dark fill (e.g. `0x010d04`, see `fillMat()` in `world/render.ts`) with `polygonOffset`, lines on top. Lines without a fill are see-through — a bug, unless the object is meant to be openwork (drones, bosses, pickups).
- **Functional colours:** drones and hostile creatures `#ffb347` (a calm Bramble is olive `#b8b060`), bosses `#ff6a4a`, gold/chests `#ffd060`, stairwells `#5cc8ff`, XP `#9dffe0`, locked doors `#ff5a3c`; water: fresh `#2ac8b0`, murky `#8a9a4a`, toxic `#b6ff3a` (glowing).
- **Controls: desktop only for now** (pointer lock, WASD, mouse). Mobile/touch development is paused: keep the existing touch code (joystick, buttons in `ui/touch.ts`) in place, but new features do not need touch controls, touch layouts or phone testing, and mobile is not considered in design decisions until the owner says otherwise.
- **Performance:** smooth on an average laptop (phones are out of scope while mobile is paused). Measure line/triangle counts (F3) on big changes.
- **Map format:** voxel maps are ordered op lists `{op:'room'|'solid', x,y,z,w,h,d}` (later ops override earlier). The future map editor will save this format — keep it.
- **Multiplayer later** (up to 8 players, Node + WebSocket, authoritative server): keep world logic (`core/`, `gen/`) free of three.js and DOM, generators deterministic, game state serialisable.

## Website
- `docs/index.html` is the game's website (Polish, CRT style, self-contained); `.github/workflows/pages.yml` deploys it to GitHub Pages with the game build under `/play/` (Pages source: GitHub Actions). Screenshots in `docs/shots/` (1280×720 JPEG, taken with Playwright from the dev build with the HUD hidden). Keep its version and numbers roughly current when big features land.

## Layout
- `core/` — pure: RNG/hash, voxel grids, meshing, noise.
- `gen/` — pure generators: dungeon, stairs, village, doors placement, reachability; open world: `regions` (256 m regions, POIs), `terrain` (heightfield, flattening), `roads`, `ruins`, `trees`.
- `world/` — runtime with three.js: player, doors/stairs, enemies, loot, NPCs, `level` (loading places, transitions), `overworld` (chunk streaming, structures, field enemies).
- `ui/` — DOM: HUD, minimap, `worldmap` (surface minimap + M map), backpack, dialogue, menu, input, touch.

## Open world notes
- Regions (rx, rz) span [rx*256-128, rx*256+128); region (0,0) holds Gridholm at the origin. New place types = new `PoiType` + a placement rule in `gen/regions.ts`.
- The planet (`gen/regions.ts`): the world wraps east-west after `NR` = 470 regions (`WORLD_W` ≈ 120 km, canonical x in [`X_MIN`, `X_MIN + WORLD_W`)); north and south it ends at the poles (ice caps from `POLAR_Z` = 25 km: no places, no forest, pale ice lines; an ice wall at `POLE_Z` ≈ 30 km that the player cannot pass). Generators take any coordinates but work in canonical ones: `regionInfo`/`chunkTrees`/`chunkRocks`/`regionVehicle` fold `rx`/`cx` with `wrapR`/`wrapC` and shift the result to the requested copy, so ids and save keys are the same on both sides of the seam; noise repeats through `fbm(..., px)` with scales adjusted by `wrapScale` (near Gridholm the land is practically unchanged). At runtime `keepOnPlanet` moves the player (and vehicles, loot; foes are dropped) to the other copy when crossing the seam and rebuilds the chunks. Distances to saved places use `worldDist` / `nearX` / `wrapDx`. Big static meshes use `localize()` (small local vertices, offset in the object position) so lines do not shimmer tens of km from the origin. The sun's path tilts with `latitude(z)` (`sunTilt`): high at the equator, endless dusk at the poles. Danger is capped at 8.
- Villages (`gen/regions.ts`): Gridholm (`GRIDHOLM_ID`, region 0,0) plus villages on a grid of 10x10-region cells (~2.5 km), sparser the further from Gridholm: `villageChance(d)` 0.8 → 0.22 and a least spacing `villageGap(d)` 1.4 → 5.2 km (the weaker candidate by hash gives way; `candidate` / `villageOfCell`, both cached), `SETTLED` from 6 to 40 km (`allVillages` lists them, ~140 per world: none in the mountains), named from syllables, never on the ice. Each is `generateVillage(villageSeed(world, poi), y, x, z, name, home)`: own layout, gates, residents with their own names (`Npc.town`; dialogue swaps 'Gridholm' for the town). Walls come in tiers (`WALL_TIERS` in `gen/village.ts`, `generateVillage(..., tier)`, default 0; not saved yet — village fortification will raise them): 0 Stake Fence (2 m), 1 Timber Palisade (4 m), 2 Stone Wall (7 m, the old voxel wall with towers, battlements and buttresses, `STONE_TIER`). Below stone the wall ring is voxels that only collide (`VillageMap.ops`; the mesh uses `shown`, without them) and `fenceDeco` in `world/level.ts` draws `VillageMap.fence` runs as leaning sharpened stakes, rails and scrap patches, gate frames (sign at `WALL_TIERS[t].gate`) and stilt watch platforms in the corners (`towers`, not solid). Wall shapes (`villageSides(seed, home)`: 4 / 6 / 8 / 12, Gridholm always 4; `wallPolygon(sides)` corners on the wall's middle line in plaza coordinates, `insideWall(poly, x, z, m)`): every shape fits the square footprint; octagon and dodecagon keep the square's flat N/E/S/W sides (apothem 36.5, so gates, `GATE_SLOTS`, roads' `GATE_POINT` are unchanged) and cut its corners; the regular hexagon (R 36.5, flat N/S at apothem 31.6) gets only N and S gates (`villageGates(seed, home)`; roads pass `home`). A polygon wall is voxel runs along its edges (`ringOps`, 4-connected), towers at its corners (every other one of a dodecagon, none by a gate; stilt platforms 3.6 m inside, stone towers straddling the corner), merlons on every other cell pair, fence runs along the edges broken at gates; buildings go round the plaza (`ringLayout`: 36 jittered angles, each building facing the middle, as far out as fits 2.5 m inside the wall, clear of the middle, the gate lanes, other buildings and the towers; shops first, then up to 6 houses); trees and walk cells stay inside (`blocked`, `walk`); `ladderIn` puts a polygon tower's ladder at the inner end of the face towards the middle. The maps draw each village's outline. Every tower has a ladder (`Tower.ladder`, `ladderOf` in `gen/village.ts`: on the face towards the plaza, `top` = deck height, `deck` rect, `stilts`); `world/ladders.ts` draws them (`drawLadder` in `villageDeco`), registers them per loaded village (`setLadders` / `dropLadders`), and climbs: E at the foot or top (`nearLadder`, `startClimb`; not with hands-only items) → `updateClimb` replaces `updatePlayer` (step on, hand-over-hand pulls with sway and two hands on the camera, swing onto the deck; W/S reverse, Space lets go; weapons away via the armed rule `climbing()`); a stilt deck is a floor (`ladderFloor` in `G.floor`) and every tower top has a rail (`ladderHit` in `G.obstacle`, open at the ladder). Lookout roofs over towers stand 2.5 m up (`lookout(..., post)`). Village state (`gen/town.ts`, pure, saved as `char.towns[villageId]`: `wall`, `given`, `fixed`, `hurt`): the elder's option 'fortify' (`renderFortify` in `ui/dialog.ts`) commissions the next wall tier (`FORTIFY` needs, gold, xp; `handOver` takes materials bit by bit, raising the tier when complete, then `reloadStruct` rebuilds the village; `loadVillageStruct` passes `wallOf(state)` to `generateVillage`). Power plants: `powerKind(seed)` generator / solar / wind (`POWER`: name, fix parts, wear per day, pay), `powerSite(seed)` (plaza-local, 17 m outside a W/E/S side, off the gates), `powerCondition` (100 − days since `lastFix` × wear − hurt; never-mended villages start at a hashed age), `POWER_DOWN` 25 / `POWER_LOW` 60. `world/power.ts` draws them (`drawPower`, rotors spin, status light, village lamps named 'lamp' go dark when down), `updatePower` in the main loop, `nearPower` / `repairPower` (E, takes the `fix` parts, pays, xp). Village raids (`gen/raids.ts`, pure, no state but the outcomes you fought): `raidSource` = nearest camp within `RAID.reach` 3.5 km; a timetable per village (`raidOf` / `raidsBetween`: every 2–5 days by hash, none in the first period); `raidOutcome` = the fought result (`TownState.raids[k]`) or a hashed roll against `RAID.hold[wall tier]` (0.45 / 0.7 / 0.9, minus strength); `raidHurt` adds `RAID.loss` 35 per lost raid since the plant was last mended to `powerCondition` (`plantCondition` in `world/power.ts`); `nextRaid` / `lastRaid` for the elder. `world/villageraid.ts` (runtime): within `VRAID.reach` 450 m of a loaded village, warns `RAID.warn` before, then runs a live raid (`LiveRaid`): 3 waves from the camp's direction (~110 m out), bandits with `home` = the nearest gate or the power plant (`ambush = -1`: culled at 240 m, not patrols); at a gate they drain the defences (`VRAID.drain` by wall tier), at the plant they add to `hurt` (at most `plantMax` 45 a raid); all waves dead → won (gold, xp), defences 0 → lost; leaving the village drops the raid (it is then settled by the roll). `raidHere()` makes the village unsafe (`foeRules.playerSafe`, the armed rule), `raidLine` in the tracker, console `raid` (a test raid, k 0, not recorded). Tribute: `tribute(r, wealth, wall)` in `gen/raids.ts` (60 + 45·strength + 0.3·storehouse value, −18% per wall tier, rounded to 10); the warning is a rider's demand (`pendingTribute`, `payTribute` in `world/villageraid.ts`; buttons in the elder's and the guard's panels, `data-tribute`; the demand in `raidLine`); paid = `TownState.raids[k] = 'paid'` (no raid); unvisited villages sometimes pay (`raidOutcome` 'paid'). In a live raid gunners at their gate shoot the wall (`shelling`: tracers, defences −0.35 a burst) and now and then a villager in the open (`killVillager`, `updateFallen`, `TownState.dead` times: `recentDead` villagers are missing for 2 days); a lost live raid drops the wall a tier (`reloadStruct`). Industries (`gen/industry.ts`, pure): `industryOf(world, poi, seed)` = farm / mine / oil / refinery / lumber / fishery / workshop / salvage (weights by distance from Gridholm and hills; a farm/workshop/salvage village with an oil village within 9 km becomes a refinery town 45% of the time); `INDUSTRY[k]` = site name, `pool` of goods it makes (the market picks up to 2), goods it `wants`, `fix` parts, `build` (refinery). `industrySite(seed, k)` (plaza-local, on another W/E/S side than the power plant, 11 m + half its depth out: inside the tree clearing). `siteCondition` = 100 − lost raids (`SITE.raid` 40, healing over `SITE.heal` 4 days) − bandit damage at the site (`TownState.siteHurt/siteHurtT`, same healing), reset by `siteFixed`; `production` = condition / 100, 0 for an unbuilt refinery (`siteBuilt`, `TownState.built`); `buildPlan` / `handOverBuild` (elder commission, `REFINERY_PAY`). `quote(..., prod)` makes a damaged site's goods scarcer and dearer, none while unbuilt. `world/industry.ts` draws the sites (fields with crop rows, mine adit + headframe + rails + cart, pumpjacks with nodding beams + tanks, refinery columns / tanks / flare or its scaffolded building site, sawmill, fish racks, workshops, salvage yard; a sign), `updateIndustry` animates by production, `nearSite` / `sitePrompt` / `repairSite` (E). Live raids send a third of the bandits at the site. Workers (`Worker` in `world/industry.ts`, figures with kits 'hoe' / 'pick' / 'hammer' / 'carry' from `world/rig.ts`): 3–4 per site walk between the site's work spots and work there (a stroke every ~1.2 s), one carries crates to the storehouse door; idle when the site does not work; placed relative to the localized village group (`localize`). Storehouses (`gen/store.ts`, pure): `STORE.tiers` Storage Shed 40 / Warehouse 100 / Depot 220 crates; fill = anchor `TownState.store {n, t}` (or a hashed start) + (`rate` 1.2 × production − `ship` 0.7) per game hour, clamped (`storeAt`); `storeFull` stops the site (workers idle, no pumps or flare); buying the village's own goods (`quoteHere` in `ui/market.ts`: stock capped by the storehouse share) and loading hauls take crates out (`takeStore`); the elder commissions the next tier (`storePlan` / `handOverStore`, `giveStore` in `ui/dialog.ts`, `data-sbuild`); drawn at a free corner of the site as a shed sized by tier with crates stacked in front by fill (`storeOf`, `storeWealth` in `world/industry.ts`). A full storehouse runs a cycle (`storeInfo`: `n`, `fullSince`, `convoyAt`): it waits `STORE.wait` 12 h for you, then the village's own convoy leaves it at `STORE.after` 0.3 of the capacity and it fills again; while it waits, `shipmentOffer` in `gen/contracts.ts` (pure, id `ship:<villageId>:<fullSince>`, `SHIPMENT`: up to 30 crates, pay ×1.25 of a haul, half deposit) is a haul to a village 2–9 km off (preferring one that wants the good), listed first by `localOffers` in `ui/contracts.ts` (store contracts and notice board); taking it loads the crates out of the storehouse (`takeStore`), so the convoy stays home. Own caravans: while the storehouse is ≥ 75% full (`glutted` in `ui/market.ts`) the village's own goods sell wholesale (`WHOLESALE` 0.8, +20 stock). The elder names the convoy time. Trade (`gen/market.ts`, pure): goods are items of type 'good' (15 `GOODS`, `GOOD_INFO` base prices, raw/made; stack 5, bulky `BULK`); `profileOf(world, poi, seed)` = 1–2 goods a village makes (from its industry's pool) and 2 it wants (the industry's wants, fuel for a diesel generator, the rest weighted by distance from Gridholm); `quote(...)` = buy/sell prices and stock (base × make 0.6 / want 1.6 × slow drift × glut from the stock shift, ± spread); `MarketState` (`char.market`, keys `villageId:good`, `{d, t}`) is the only state: `trade` adds crates sold (+) / bought (−), shifts decay with `MARKET.half` 36 h, `tidyMarket` prunes. On the multiplayer server this state is shared = global economy. `ui/market.ts` (merchant option 'trade' in every village, wide `#dlg .panel.wide` table): buys into the backpack then trunks of owned vehicles within 90 m (`stores`), sells from them, one crate at a time so each moves the price; `char.ledger[villageId]` records prices seen (best price elsewhere per good, distance, age); hearsay names the 2 nearest unseen markets within 9 km and what they want. Delivery contracts (`gen/contracts.ts`, pure): `offersAt(world, poi, seed, now)` = `CONTRACT.perVillage` 2 offers per posting (`CONTRACT.period` 12 h, fixed at the posting's start): supply (a good the village wants, n crates, pay = its sell price × `premium` 1.35) or haul (a good it makes to a village 2–9 km off, preferring one that wants it; pay per crate by distance and danger; deposit = n × buy price). `payFor` (a finished haul returns the deposit). `ui/contracts.ts` (merchant option 'contracts'): `char.contracts` (max 3) and `char.taken` offer ids; taking a haul pays the deposit and loads the crates trunks-first (`putAway(..., true)` from `ui/market.ts`, whose `stores` / `carried` / `takeFrom` take a place); handing over at the destination's store pays per crate and moves its market (`trade`); `updateContracts` fails overdue ones; `contractLines` in the tracker, `contractMarkers` in `questMarkers` (compass, maps). NPC caravans (`gen/caravans.ts`, pure, no state): every road edge has a timetable (`CARAVAN`: period 6–16 h by hash, 25% of departures skipped, departures alternate a→b / b→a, speed 7 m per game minute along `edgePath`), `caravanOf(world, edge, k)` (cargo: what home makes, preferably what the destination wants; 1–3 wagons), `departures`, `onRoad`, `caravanS`; `caravanShift` (departure −n, arrival +n, × `weight` 0.25, decaying with `MARKET.half`) feeds `quote` (`caravans` flag), `lastArrival` feeds the merchant's news. a caravan is a convoy (`CONVOY`: gun jeep, Mastodon truck with the cargo 12–30 crates, gun jeep; `back` offsets 0 / 8.5 / 17 m used by `caravanS`). `world/caravans.ts` draws the convoy (`convoyModel` from `world/vehicles.ts`: body, wheels, roof cannon; per-vehicle boxes `SIZE`; the jeeps' guns `aimGun`/`CONVOY_GUN` turn to the nearest raid bandit or, with none left, a barricade within 30 m, and fire: tracer, noise, `shootBandit` without bounty) on `regionRoads` near the player (`updateCaravans` in the main loop, rescans twice a second, keeps to the right, pitch from the terrain), `caravanHit` in `G.obstacle` plus a shove, `nearCaravan`; `ui/caravan.ts` (E): route, cargo, arrival time, buy crates at the midpoint of the two markets' prices (`char.caravans[id]` = crates bought). Raids (`world/caravans.ts`, runtime only: `Live` per caravan: `delay`, `hp`, `raid` bandits, `rolled`): a caravan coming within 160 m rolls once (`CARAVAN_RAID`: 12% + 4% per danger, from danger 1.2, through `mayspawn`); `startRaid` spawns 3+ bandits beside the lead wagon (`sight` 60) and a roadblock 16 m ahead (`placeRoadblock`); the convoy also halts while a barricade stands within 9 m ahead of it; while any are within 45 m the caravan halts (`delay` grows) and loses `drain` hp per bandit per second; at 0 it is plundered (`char.caravans['lost:<id>']`, never drawn again, nothing to buy); when the raid is over with at least half the bandits killed it is saved (gold, xp, `saved:<id>` → 30% off its cargo). Escort (`char.escort`, `takeEscort` from the drover's panel while the caravan has done < 35% of its road, pay `escortPay`): a guaranteed raid past 30% of the road, a second one (50%) past 72%; `escortTick` pays on arrival if you are within 400 m of the destination, fails if the caravan is plundered or you stay 350 m+ away for a minute; `escortLine` in the quest tracker. Maps: `mapWagons` (minimap and map, gold = your escort, red rim = raided), the big map draws every caravan on roads between known villages from the timetable (`caravanPos`, at most 2 new road paths per frame via `pathKnown`). The hero's house (Gridholm only): the house at plaza (3, 43) is `YOUR HOUSE` (`Building.mine`; the sign reads FOR SALE and the door is padlocked until the elder sells it: option 'house', `renderHouse` in `ui/dialog.ts`, `HOUSE_PRICE` 750 in `data/npcs.ts`, `char.houses` village ids; saves that already used the home chest get it free; the chest, bed and waking in your bed need it), `VillageMap.house` holds its bed and chest (world); `world/home.ts`: E at the chest opens the transfer window on `char.containers['home:chest']` (`HOME_CHEST_SLOTS` 24), E at the bed sleeps (`sleepSpan` / `SLEEP` in `data/survival.ts`: from 20:00 to 06:00 until 07:00 with full HP, else a 2 h nap; kcal/water/stomach run on at the sleeping rate); death and recall to Gridholm arrive by the bed (`Arrival` tavern `bed`). Only Gridholm (`VillageMap.home`) has Kuba's yard for now; every village has a notice board. Safe zones, holstered weapons and the no-spawn margins apply around every village (`villageContaining`, `villageDist`). Death and the Recall Beacon take you to the nearest village you have explored (`toVillage`); console `home` = Gridholm. Future multiplayer: villages (and later crash sites) are the spawn points.
- Wall-walk and defence works: `WALL_TIERS[t].walk` (palisade 3.3 m, stone 5.6 m; none behind a stake fence). `generateVillage` gives `VillageMap.walkway` (`WalkRun`: the fence runs on the wall's middle line trimmed clear of corner towers (4.5 m) and a stone wall's gate towers, `nx/nz` into the village, deck `WALK.in0`..`in1` 0.5–1.7 m inwards), `walkLadders` (`Ladder.walk`: one per run, two on runs > 28 m, nudged to free ground by `clear`) and `mounts` (`Mount`, turret spots on the wall top: by the gates first, then run middles, `MOUNTS_MAX` 6), all from `wallWalk` in `gen/village.ts`. `world/walkways.ts`: `setWalkways` / `dropWalkways`, `walkFloor` in `G.floor`, `walkHit` (inner rail open at ladders, end rails) in `G.obstacle`; ladders register with the towers' (`ladderHit` skips `walk` ones); `walkwayDeco` in `world/level.ts` draws deck, planks, posts with knee braces, rail and toe board. A palisade's stakes stay 3.2–4 m so you shoot over them from the walk. Defence works (`gen/town.ts` `WORKS`, `workPlan` / `handOverWork`, `TownState.works` / `wgiven`, `worksOf`): 'turret' (Turret Kit + circuits + wire + scrap, up to the village's mounts; `BASE_ITEMS` no longer hides the kit), 'siteGuard' and 'plantGuard' (sandbag barricades); the elder's fortify panel lists them (`defenceText`, `defenceRows`, `giveWork`, `data-work`) and `reloadStruct` rebuilds. `world/wallguns.ts` (runtime; loads through `villageHooks` in `world/overworld.ts` to keep overworld free of the player/enemies import chain): a braced platform with a steel ring, ammo box and twin-barrel head per turret, targets hostile `foes()` within `WALL_GUN.range` 60 m in sight (`rayWorld`), fires via `damageFoe`; idle it sweeps outwards. `world/siteguards.ts` `drawGuards` (with the village group): sandbag walls 3 courses high round the site / plant (open towards the village), steel hedgehogs and spiked timber outside, `guardHit` in `G.obstacle`. `GUARDED`: bandit damage at a guarded site/plant in a live raid ×0.35, a lost raid's damage ×0.5 (`siteCondition`, `raidHurt`).
- Processing economy: raw goods come from each village's land (its industry's `pool`; mines and fisheries also dig `sand`), made goods as before, and 9 processed goods (`GOOD_INFO[g].proc`, `PROCESSED`: steel, copperbar, plastic, glass, cable, boards, parts, alloy, propellant) that no industry makes and that pay well over their inputs (test: every recipe ≥ 1.2× at base prices, every input dug or made). Farms have `fertility(world, poi, seed)` 0.6–1.6 (`gen/industry.ts`; `production` is multiplied by it, so rich fields fill the storehouse faster and sell food cheaper and in bulk: `quote` divides the make factor by √prod above 1). Works (`gen/plants.ts`, pure): `PLANTS` (smelter, refinery, glassworks, wiremill, electronics, machineshop, foundry, chemworks: recipes, `batch` minutes, build `needs`, `fee`, xp), `PLANT_SLOTS` 2 per village, any village any kind; `TownState.plants` (`PlantState` {k, rec, inp, out, t}) and `pbuild` (the one going up); `runPlant` settles batches by game time (one per batch while fed and the bay has room; idle time does not bank), `feed` / `collect` / `setRecipe`, `HOPPER` 40, `OUT_CAP` 40; `startPlant` / `handOverPlant` (materials bit by bit, the fee paid at the end). `plantSite(seed, industry, slot)` = the W/E/S side with neither the power plant nor the industry site. The elder's option 'works' (`renderWorksPanel` in `ui/dialog.ts`: the land's goods and fields, `data-pnew` to start one, `data-pgive` to hand over / pay). `world/works.ts` draws each kind (blast furnace, columns and flare, kiln dome, wire drums, electronics block with mast and dish, sawtooth machine hall with a crane, foundry with crucible gantry, spherical tanks), a hopper and an output pallet, staked plots and the building site; `updateWorks` shows the glow while it runs; `nearWorks` / `worksPrompt`, E → `ui/works.ts` (pick the recipe, load inputs from the backpack and trunks by the village via `carried` / `takeFrom`, collect into the trunks first).
- Power (`gen/energy.ts`, pure): works need electricity. `balance(world, poi, seed, state, t)` = made (`baseKw`: the village's own plant, `BASE_KW` by `powerKind` × condition, solar by `daylight`, wind by `windAt(seed, t)`; 0 once down) + stations; the village takes `VILLAGE_KW` 25; the rest powers the works in build order while each has work to do (`DRAW` kW per kind; `powered[]`). Stations (`STATIONS`: Solar Farm 70 kW by day, Wind Farm 80 × wind, Coal Power Station 140 while its bunker has coal (a crate per 2 h), Diesel Generator Bank 110 on fuel (a canister per 2.5 h); `STATION_SLOTS` 2, `BUNKER` 30) are `TownState.stations` (`StationState` {k, on, fuel, t}: `fuelAt`, `loadBunker`, `switchStation`), built through the same `pbuild` commission as works (`isStation`, `specOf` in `gen/plants.ts`); `stationSite(seed, industry, slot)` = the far end of the power plant's side (0) or the industry site's side (1). `runPlant(p, now, pw)` / `feed` / `collect` / `setRecipe` take `pw` = `poweredAt(...)` (a batch without power at its end passes idle); `powerOf(w)` in `world/works.ts`. The elder's works panel shows the balance (`powerText`) and offers stations first. `world/stations.ts` draws them (panel rows and an inverter; three turbines with rotors turning with the wind; boiler house, cooling tower, stack, coal heap and conveyor; generator containers, stacks and a fuel tank), a lamp while it makes power, `nearStation` / `stationPrompt`; E → `ui/stations.ts` (output, the balance, the works' power, load the bunker, switch on/off).
- Village life (`gen/growth.ts`, pure; **not wired into the game yet**, tuned with `npm run sim`): `LifeState` per village (pop, food, gold, guards, happy, `built` counts of house / farm / works / granary / barracks / market, one `building` at a time, wall tier, `focus`, `log`), `LifePlace` (seed, danger = `ringDanger` of the distance from Gridholm, fertility, `foodIndustry`), `LifeMarket` multipliers. `stepLife` = one game hour: farms feed (`food` × staffing × fertility), jobless forage, everyone eats `LIFE.eat`; works earn gold (each further one `WORKS_FALL` less), upkeep × `sprawl` (1 + 0.025 per building: the soft cap on growth) + guard wages; food traded daily (sell beyond 5 days' need, buy down to 3); logistic growth to the housing by mood, starvation, leaving when unhappy, `minPop` floor; raids rolled from `hash(seed, day)` (`raidOdds`: chance by danger × wealth, strength vs wall/guards/mood); the governor (`decide`, every `LIFE.decideEvery` h) picks by needs and focus and only builds what pays (`pays`), `shrink` pulls down what does not (or ploughs fields under in a food glut). `runLife` catches a village up. ~1.2 µs per village-hour. Findings so far: worlds grow ~7× in ~9 months and level off (~33k people over ~130 villages), no village dies out; the world's food ends in a glut (price at its floor) until real inter-village trade and player demand come in.
- Civilisation layer (`gen/civ.ts`, pure, on top of village life; **not in the game yet**): `MAT` (28 materials incl. new rares bauxite / sulfur / lithium / rareearth / uranium, processed aluminium / batteries, exotic `relic`), `RECIPES`, `TIERS` Settlement → Town → City → Metropolis (housing `cap` via `LifeState.popCap`, `rise` goods, daily `use` per 100 people, works allowed), `WORKS` (batches, cost, upkeep, `wear` in parts/cable), `DIG`. `newCiv(seeds, players)`: villages dig their industry's goods + a rare deposit likelier further out (`rareOf`), 20 projects (`PROJECT_NAMES`) anchored by farthest-point sampling, every village joins its nearest (region), 3 stages each (the last needs relics), the Chariot (`CHARIOT_STAGES`, players only). `civDay`: 24 h of `stepLife` per village (goods factor from its digs' prices), then per village: dig (output follows the price), run works while the margin pays, buy daily use (unmet sours the mood), gather for the next tier, build a works (score: margin × own needs × inputs available × local inputs ÷ makers; a poor village's works can be paid by the region's richest, `invested`), give to its project; players (`playersDay`): purse, relics 2/day each, haul for the Chariot, relics to projects' last stages and to cities rising. One shared market (`Pool`: stock → price by `TARGET`, 0.6% leaves a day). Findings (world 12345): without players the economy runs (~65 works of 11 kinds, all processed goods have buyers) but stalls after ~1 year (119/127 still settlements, unmet use rising to ~38%), only the core region's project reaches its relic stage and 18 of 20 regions never start; with 4 players the Chariot is done by day ~230 and a regional wonder is finished, relics are far too plentiful (thousands found, dozens used).
- The campaign (`gen/campaign.ts`, pure, from the world seed; **not in the game yet**; the design direction after the economy experiments: simple fixed rules tuned to target play times, not a simulated economy): the Chariot (`gen/shuttle.ts` STAGES × `CHARIOT_SCALE` 1.85 × `CHARIOT_WEIGHTS` light → heavy), 20 wonders (`WONDER_KINDS`: name, regional perk, the part the Chariot may need; anchored by farthest-point sampling over the villages, each the nearest ruin to its village; 3 stages: clearing (timber, tools), the machine hall (2 processed goods), the heart (relics + 1 processed; `TWISTS` texts), scaled by danger), 2 surprises (`SURPRISE`: after Chariot stage 1 a wonder 8–13 km out, after stage 3 one 20–30 km out, picked by hash; `Blocker` with the crew's words and a clue: direction, km, the village it is near) and the finale (launch-night defence, 6 waves). `visible(c, progress)` shows only what has happened (surprises met, a wonder's current stage). Timing (player bot, `PACE` guesses): ~80 h alone (80–90 over 8 worlds), surprises at ~20 h and ~53–58 h, ~50 h for 2, ~32 h for 4; side wonders 7–20 h each (~270 h). Design targets from the owner: 80 h alone, 2 surprises during the build + 1 at the launch, surprises not plannable in advance.
- The village computer: furniture `'terminal'` in the elder's hall (`B()` in `gen/village.ts`, drawn by `drawPiece` in `world/houses.ts`: desk, monitor with text lines, keyboard, case). `world/terminal.ts` `nearTerminal` (stand ~0.6 m in front of it, facing `n`) → E in `world/interact.ts` → `ui/terminal.ts` `openTerminal` (dialogue panel, `data-tt` tabs, `data-tclose`, clicks via `terminalClick` in `ui/dialog.ts`): VILLAGE (industry, site, storehouse, works, defence, raids), POWER (`balance`, sources, works' power, 24 h forecast), TRADE (contracts, recent trade from `char.market` shifts, caravans on its roads, its market), VILLAGES (explored, in the ledger or road-linked: industry, goods, wall, works), CHARIOT (stages). Read only.
- The shuttle (`gen/shuttle.ts`, pure): POI type 'hangar' (`HANGAR` rect, `HANGAR_ID` = packId(0, 0, 1), added in `baseInfo` for region 0,0; the N and E neighbour ruins keep to the side away from it), `STAGES` hull / engines / avionics / shield / fuel, each needing processed goods only; `char.shuttle.given` (save), `giveToStage`, `stageRows`, `stagesDone`; the locals call it the Chariot of the Ancients (`CHARIOT`); nothing is sold at the hangar: `updateChariot` in `ui/shuttle.ts` (main loop) unloads whatever a stage still needs from your backpack and trunks within `SHUTTLE.reach` 25 m of the hangar, xp per crate, no gold. `world/hangar.ts`: `hangarOps` (voxel side and back walls, a low plinth), `drawHangar` (arched ribs and skin, back gable, slid-aside doors, gantry crane, lamps, crates, the project desk `deskOf`), the shuttle lofted by stage (bare ribs → plated hull, engine bells, lit cockpit windows, belly tiles, glowing fuel lines); `loadHangarStruct` in `world/overworld.ts`; E at the desk (`atShuttleDesk` in `world/interact.ts`) → `ui/shuttle.ts` (the stages, read only; `reloadStruct(HANGAR_ID)` when a stage completes). Gridholm's elder's lore adds `LORE_SHUTTLE`. Maps draw the hangar as an arch. The flight itself is still to come.
- House doors (`world/housedoors.ts`, runtime only): every building's door is a leaf on a hinge (`setDoors` / `dropDoors` with the village, `updateHouseDoors` in the main loop swings it), E opens/shuts it (`nearHouseDoor`, `toggleHouseDoor`; the choice holds until the next dawn 06:00 or dusk 21:00, `phase`). Shops, the hall and the tavern stand open by day; at night shops and the hall are locked, the tavern only shut; folk's houses are shut, openable by day, locked at night; the hero's house is locked until bought; from inside a building its door always opens. A leaf not fully open collides (`doorHit` in `G.obstacle`), a shut one stops rays (`doorRay` in `G.rayBlock`); NPCs pass through.
- Village houses (`Building.walls`, `HOUSE` in `gen/village.ts`): timber-framed like bases, thin walls (0.25 m) on the footprint's edge with a doorway (`doorW` x `doorH`) in the door side, not voxels; `world/houses.ts` draws them (`drawHouse`: planks, sill, posts, windows with shutters, open door, gabled shingle roof with eaves and boarded gables, chimneys, shop awnings, the hall's bell turret) and collides them (`setHouses` / `dropHouses` per loaded village, `houseHit` in `G.obstacle`, `houseRay` in `G.rayBlock`, `houseSolid` in `G.solid`, and in `npcFree`). Furniture (`Building.furniture`: `Furn {k, x0, z0, x1, z1, h, n}`, `FurnKind` counter / table / bench / stool / shelf / barrel / crate / sack / anvil / hearth / bed / desk) is laid out per role in the building's own frame (u from the back wall to the door, v across) in `B()`; `drawFurniture` in `world/houses.ts` draws it (wares on the counters by role), `setHouses` collides every piece but stools (`furnSolid`). The hero's house gets its own set (table, stool, shelf) beside the bed and chest. Gridholm's residents: Marta (tavern), Elder Maciej, Oskar (blacksmith), Zofia (store), Jan (food), Kuba (vehicle dealer), Jakub (captain of the guard), villagers from `VILLAGER_NAMES` (12; other villages 8 from `FOLK`). Every village has a guard (`NpcRole` 'guard', spear and shield on the figure, walks `Npc.route` = walk cells within 6 m of the wall, stops when you are near; option 'watch' → `renderWatch`: wall tier and `raidNews`).
- Line styles: dungeons keep the original 1 m grid on every face; surface structures use the outline style (`meshVoxels(..., outline)`: folds and edges, 2 m floor tiles, 4 m wall seams). Decorative non-voxel shapes (roofs, lookouts, gate arches, rocks) go through `PropBatch` — collision stays on voxels.
- Roads (`gen/roads.ts`, pure): a network between villages only. `network(world)`: each village joined to its 1–3 nearest (hash; Gridholm 3) within `linkAt(v)` (`ROAD.link` 7 km near home, ~16 km far out where villages are sparse), union of the pairs. `edgePath` (cached): from the gates facing each other (`gateToward`, `gatePoint`), A* (`findWay`) on a 32 m grid (`ROAD.cell`) within `ROAD.margin` 1 km of the pair's box (retried at 2.8×): cell cost lazily per cell, mountains closed above mask 0.3 and costly below, the ice closed, ruins/camps/wrecks closed within 15 m and costly to 80 m (other villages 30 m); then string-pulled and Chaikin-rounded. `regionRoads(world, rx, rz)` lists the roads crossing a region (id `road:a:b`, `half` 2.3); `featuresIn` takes each road once and only its stretches near the rect. Drawn as a translucent shaded strip (`ROAD_FILL` in `world/overworld.ts`), trails too, no lines. `villageLinks` for maps/tests.
- Trees (`gen/trees.ts` pure, `world/trees.ts` models): candidate spots never change (`treeTrunks`, used for vehicle placement); each spot's kind comes from its own hash: pine (high ground), broad (default), and the big T-01 Ancient Twisted, T-02 Umbrella and T-03 Hollow Arch (walk-through; one collision circle per leg, `Tree.cols`). Big trees (`TREE_SPAN`) clear small ones around them, also across chunk borders, and keep off roads and places. Models are faceted tubes, crowns and canopies in a PropBatch with a far LOD.
- Water (`gen/water.ts` pure, `world/water.ts` runtime): lakes per region (`regionLakes`: a lake in ~45% of regions (70% in low ground) plus a smaller pond in ~40% (own RNG stream); never on the ice, the mountains, near places, roads or each other), each a still surface (`level`) over a bowl carved into the terrain (`lakeBed`, applied in `Terrain.exact` via `Features.lakes`) with a low lip just past the shore so water never spills; kinds mostly fresh / murky (many low valleys) / toxic (often within 180 m of ruins, rarely anywhere). Ask `Terrain.water(x, z)` → `{ level, depth, kind }`. Wells: every village well (its 2×2 voxel only collides, left out of `shown`; drawn by `drawWell` in `villageDeco`) plus wild wells (`regionWells`, ~2 regions in 5, drawn per chunk, solid). Player: wading slows, deeper than `SWIM_DEPTH` you swim at the surface with weapons away (diving later), toxic water hurts. E at water/wells drinks (fresh +8 HP, murky may hurt, toxic refused) or fills an Empty Flask (→ Flask of Clean / Murky Water; the flask is kept when drunk). Vehicles stop at water deeper than their spec `wade` (Scout 0.6 m, Mastodon 1.2 m); creatures and bandits avoid deep water. Thirst/food will build on this (see `useWater`). Rivers: not yet.
- Ruins are alien temples (`gen/ruins.ts`, 40 m square): a cross plan with a round inner wall and four broken arms; the crypt with the stairwell down sits behind a stepped portal between two leaning pylons that carried a great ring; an avenue of obelisks leads in. Collision and the stairwell are voxels (`ops`); the alien shapes are `RuinMap.temple` (temple-local u/v coordinates, `templeXZ`) drawn by `world/temple.ts` as PropBatch solids that enclose the voxels. Everything is rolled weathered: snapped pylons, a fallen or missing ring, broken/leaning obelisks with their tops lying by them, ragged tops, fallen blocks, rubble.
- Crash sites (POI type `wreck`, `WRECK_LEN` x `WRECK_WID` rect along x or z; ~14% of regions, ≥ 400 m from villages and 110 m from other places, `packId(rx, rz, 3)`, placed in `regionInfo`): `gen/wreck.ts` (pure) `generateWreck` gives hull-local frame (`wreckXZ`, u along the ship with the nose buried, v across), collision voxels as 2 m slices inscribed in the octagonal hull (`hullR`, `hullY`, `octHalf`), a side hatch with a vestibule and a stairwell going down towards the tail; `world/wreck.ts` `drawWreck` draws the hull (open over the hatch), nacelle with rings, fin (maybe snapped), cockpit windows, crack, hatch collar and ramp, debris. Inside (`loadDungeon` picks `gen/ship.ts` `generateShip` when the dungeon's POI is a wreck): mirrored bays along a straight spine (`SHIP_CX`): engine room with reactor drums and an elite guardian at the stern, airlock with the stairs up (`V` portal), crew cabins, side compartments, cargo hold, bridge at the bow; bulkhead and stub doors, crash debris; one level only (`hatch: null`, no sector stairs). Ship chests add salvage (scrap, electronics, rare cores, food, parts). Robots guard the wreck: `DungeonMap.guards` (own RNG stream in `generateShip`: scouts in the spine and cabins, guardians in side compartments and on the bridge, guardians + a repair drone or a sentinel in the cargo hold, an assault construct or sentinel with a repair drone in the engine room); `loadDungeon` sets the robots' env `{ indoor: true }` (no spawning, no despawning, walls and ceilings block them: `roomFor` checks the footprint up to the robot's height `ROBOTS[k].h`, so the heavy ones stay in tall rooms) and calls `spawnGuards` at the wreck's danger (≥ 2); only 2 stray drones. `updateRobots` runs underground too and moves their bolts there (`updateBolts`).
- Angled dungeon decoration (`world/dungeondeco.ts`, from `DungeonMap.boxes` and `style`): all above head height, collision stays on voxels. Temple rooms (h ≥ 5) roll chamfered ceilings, pointed vaults over big halls, raking struts with corner squinches, or plain walls; ship corridors and rooms are always chamfered (octagonal section) with hull ribs, gaps at door frames.
- Mountains (`gen/mountains.ts`, pure): `mountainMask(world, x, z)` 0..1 (low-frequency fbm at ~1 km scale, ~24% of the land, none within `MOUNTAIN_CLEAR` 1.4 km of Gridholm), `mountainLift` (ridged noise + peaks, up to ~170 m) added in `Terrain.base` before the polar blend. Villages (`villageOfCell`), ruins, camps, wrecks, lakes, wells and roads (closed above mask 0.3, costly below) keep off them via `onMountain`. `Terrain.forest` thins above ~50 m. Chunks reaching above `SNOW_LINE` (115 m) draw pale lines; the maps shade high ground paler. `world/farpeaks.ts`: a silhouette ring (radius 145, fog-free, travels with the camera) of the real mountains out to 3.2 km, rebuilt after moving 120 m.
- Trails (`gen/trails.ts`, pure): `regionSummit` (the region's highest point after a hill-climb, ≥ 70 m, kept by the region it stands in; `Mount <name>`), `regionTrails` (only peaks with no higher summit within 450 m): from `trailhead` (nearest foot: mask < 0.06, h < 36, no lake) a greedy climber (8 m steps, aims for the top at ~0.7 × `TRAIL.grade`, turn penalty, legs ≥ `LEG_GAP` 14 m apart) up to the summit; the height profile (`Road.h`) is the smoothed ground clamped to `TRAIL.grade` 0.36 both ways; trails needing a cut/fill > 9 m are dropped. A trail is a `Road` (`gate: 'trail'`, `h`, `name`, `half` 2.2 so the 2 m lattice sees the bench), gathered by `featuresIn` from regions within 1.5 km, so trees, rocks, plants and claims avoid it; `Terrain.exact` applies the nearest trail's `trailHeight` with `TRAIL_BLEND` 9 m. Drawn as dashed edges in `buildChunk`; `world/trails.ts` markers (signpost, cairns every 7 points, summit cairn + pennant). HUD place names at the trailhead and summit; maps show them (dashed on the area map).
- Caves (`gen/caves.ts`, pure): `rawCaves` (≤ 1 mouth per mountain region, ~45%: a flank spot with a gentle approach and a steep face behind) and `regionCaves` (linked: two mouths that are each other's nearest within `LINK_DIST` 650 m join one system with `LINK_CHANCE`; `Cave.sys` / `name` are the system's, `mouth` 0/1, `other`). `world/caves.ts` draws the open mouth (arch, dark tunnel, boulders; `caveHit` in `treeHit`); E at it (`atMouth`) → `enterCave` in `world/level.ts`.
- Cave interiors: `gen/cavegen.ts` `generateCave(seed, 1|2)` (pure, seed `hash(world, sys, 0xca11)`): chambers with lobed outlines, a spanning tree + loops + dead-end pockets, winding tunnels (midpoint displacement, varying radius); per-metre `floor` / `ceil` fields (flat middle, floor curling up and vault coming down at the sides; heights blended softly between elements so there are no steps), exits on the west (and east) edge, chests in pockets, crystals, stalagmites/stalactites. `world/cavelevel.ts` `buildCave`: `c.dungeon.cave {name, mouths, from}` (loc stays 'dungeon', `ruinId` = system id, so chest keys work), floor + vault nets over a double-sided fill, `G.ground` = the floor field, a coarse `VoxelGrid` of the rock as `G.space` (walls, vault, shots, minimap via `setMiniOpen`), `G.obstacle` = low headroom + stalagmites, drones on `W.spawnCells`; E at an exit (`caveExitNear`) → `exitCave(i)` in front of mouth i (the far side for a linked system). Test: `tests/cavegen.test.ts` walks every chamber and both exits.
- Terrain LOD: chunks more than 2 chunks away draw grid lines every 4 m instead of 2 m. Distant mountains (`world/sky.ts`) are a fog-free ring that follows the camera.
- Structures are `VoxelGrid.surface(...)` grids: their footprint replaces the terrain (terrain mesh has a hole there, collision uses voxels only).
- Dungeon seeds: `hash(world, ruinId, depth, gx, gz)`; save keys `ruinId:depth:gx:gz`. Save format v3 (`gridWorld.character.v3`; older `gridArena.*` keys are still read), migrations in `save.ts`.
- Vehicles (`data/vehicles.ts` specs incl. price and `enclosed`, `world/vehicles.ts` models/driving): RTV-1 Scout and HTV-6 Mastodon. Bought from Kuba, the dealer at the yard outside Gridholm's north gate (`YARD` in `gen/vehicles.ts`, first free bay), or found abandoned in the wilds (`regionVehicle`, deterministic per region, id `found:rx:rz`; claimed on first use, trunk loot rolled then). Only closed cabs (`enclosed`) shield the driver from drone contact damage (`foeRules.shielded`). Vehicles stay out of the village and ruins. Owned ones are saved in `char.vehicles`.
- Vehicle parts (`VehicleParts` in `data/vehicles.ts`, saved per vehicle): wheel conditions (-1 missing, 0 wrecked), engine, roof cannon. Missing/wrecked wheels or a dead engine stop the vehicle; wear lowers speed. Found vehicles come damaged (rolled from the region seed, visible before claiming). The service window (`ui/service.ts`, E at the vehicle's front) fits Light/Heavy Wheels, Engine Parts and the Vehicle Cannon. Kuba sells parts (`PART_PRICE`) and buys them back at `PART_BUYBACK` (20%); vehicles sell back for half price scaled by condition (`resaleValue`), trunk must be empty.
- People in vehicles: `SEATS[model]` in `data/vehicles.ts` (3 each: driver 0, passenger, gunner standing at the roof cannon (`gun`); `y` = hip, or feet for the gunner). `Vehicle.riders` (`Rider {who, fig}`), `seatRider` / `unseat` / `freeSeat` / `aboard` / `seatPoint` in `world/vehicles.ts` draw a figure per occupant (sitting with legs forward, the driver's hands on the steering wheel, the gunner turning with the turret in `pose`); getting in seats you ('you', hidden in cockpit view), the HUD shows `n/3 aboard`. Raiders seat a driver and gunner (+ a passenger in a Mastodon), caravans draw a crew (`convoyModel(..., crew)`). Hits: `HULL_BOXES[model]` (the solid parts: the Mastodon cab is built round open windows and a roof hatch) + wheels vs `riderBoxes`; `rayVehicle(v, o, d, max)` returns the nearer of the body (seat -1) or an occupant. `shoot()` / `fireCannon` use `rayRaider` → `hurtCrew` (raider `crewHp` per seat: a dead gunner silences the cannon, a dead driver makes the crew bail out (`bailOut`) and leaves the vehicle whole and claimable, `abandonRaider`) or `hurtRaider` (the vehicle). Bandit bolts aim at `seatPoint(driving.v, 0)` and `updateBolts` tests `rayVehicle` per step: seat 0 hurts you, the body damages the vehicle.
- Hull and fuel: `VehicleParts.hull` (hull points, max = spec `hull`) and `fuel` (litres, max = spec `tank`). Hull damage goes through `damageVehicle` (bandit/raider bolts, rams, crashes, and contact damage a closed cab absorbs via `foeRules.shieldHit`); at 0 the vehicle is wrecked (immobile, smoulders, driver thrown out) until Hull Plating is fitted in the service window. Fuel is tracked and shown but not burnt yet (`FUEL_BURN = 0` in `data/vehicles.ts`). Old saves get a full hull and tank (`upgradeParts`).
- Hero name: `char.name` (default '', set in the main menu's `#heroName` field, `cleanName` in `ui/menu.ts`; Play refuses without one; New character clears it). Dialogue passes texts through `here()` in `ui/dialog.ts`, which swaps `{name}` for it (and 'Gridholm' for the town): use `{name}` in `NPC_INFO` hellos and `VILLAGER_LINES`.
- Game clock (`core/time.ts`, pure): `char.time` in game minutes, 1 real second = 1 game minute (a day = 24 real minutes), runs whenever the game is not paused. Day/night in `world/sky.ts` (`updateSky`): sun and moon on opposite sides of one orbit, stars fade by day; a sky dome (`dome`, a ShaderMaterial drawn at the far plane, no depth: zenith → horizon gradient plus a glow round the sun) goes black at night → a bright hazy green by day (`DAY_Z` / `DAY_H`) → amber at dawn and dusk (`DUSK_*`, by `twilight`); the fog and `scene.background` take the horizon colour and lie further off by day; the sun's disc brightens, the moon's globe takes the zenith colour. Underground stays black. `daylight(t, tilt)` drives solar output too (gen/energy.ts). The notice board posts every 6 game hours (`boardPeriod`, `char.board.stamp`): the two oldest untaken offers per posting are replaced.
- Weather (`gen/weather.ts`, pure): `weatherAt(world, x, z, t)` → `{kind, cloud, rain, fog, storm, wind, windDir}`; every `WEATHER.cell` 3 km cell rolls a kind per `WEATHER.slot` 4 h (`KINDS` weights: clear 40, overcast 20, rain 18, fog 12 (×2.2 in the early morning), storm 10), numbers blended over the 4 nearest cell centres and faded from the last slot over `WEATHER.fade` 45 min; no thunder over the ice. Looks only for now; `weatherAt` is the hook for later effects (lightning strikes, storms tearing roofs, rain on roads, clouds on solar output). `world/weather.ts` (runtime, surface only): `seen` eases towards the forecast; rain streaks in a box round the camera (`N` 1600, slanted by the wind), faceted cloud banks drifting at 60–80 m (veiled by fog), forked lightning bolts 60–170 m off in a storm (`strike`, a flash that lights the sky, a tremor of the camera; no sound yet); `forceWeather` (console `weather clear|overcast|rain|fog|storm|auto`). `world/sky.ts` reads `seen`: clouds grey the dome and hide the stars, sun and moon; fog pales the horizon and pulls the fog in; the flash brightens it. Fog hides the horizon rings and far peaks (main.ts). The HUD clock shows the weather ("· Rain").
- Creatures (`data/creatures.ts`, `world/creatures.ts`), open world only, unsaved like drones: Ravager packs (flank, dash, bite, retreat; plains), Bramble (territorial ~11 m, head-down charge with knockback, 60% less damage from the front; valleys h < 9), Leechwing (circles ruins/high ground, stalks, dives; flies over terrain), Gnawer (rat-like, long incisors, spined lashing tail; nests of 3–8 turn up anywhere, ~30% of spawns: scurry and sniff, the whole nest swarms in zig-zagging, bites and skitters off, the last one or two flee). Models are PropBatch bodies with origin at the body centre so weapons (`t.g.position`, `t.r`) and EMP (`t.p`) work unchanged; `damageFoe` routes them to `hurtCreature`; at 0 hp `fallCreature` takes them out of `W.creatures` and `updateDead` animates the body (runners slide and roll onto a side kicking, a Bramble folds its legs, a Gnawer flips over, a Leechwing falls tumbling and lands with its wings spread; colour drains, `LIE_C` s, sinks, `removeCreature`). Console: `spawn ravager|bramble|leechwing|gnawer`.
- Danger and the threat budget: `gen/danger.ts` (pure) `dangerAt(world, x, z, nearRuin)` grows in rings round Gridholm: `ringDanger` of the distance from Gridholm's walls (`DANGER.rings`: 0 to 60 m, 1 from 600 m to 1.6 km, 2 at 4 km, 3 at 8, 4 at 13, 5 at 19, 6 at 26, 7 at 34, 8 at 44 km), the distance warped by `wobble` (angular harmonics, ±16%) so the rings are blobs; every village is a refuge (0 within `DANGER.safe` 60 m of its walls, `villageFar`, the ring's danger returning over `DANGER.refuge` 500 m); +0.6 near ruins, capped at 8. Higher danger = more and bigger encounters and a larger share of heavy robots (`pickGroup` weights grow with lv past each kind's `min`); `overworld.danger()` wraps it and the HUD place name shows it ("Wilds · danger 3"). Every spawner (creatures, robots, bandit patrols, raiders, ambushes) asks `mayspawn(cost, lv, heat)` in `world/threat.ts`, which paces encounters: one at a time, then the next waits `gap(lv)` (~45-75 s at danger 0, ~20-30 s at 8; gunfire `heat` shortens it), nothing new while anything hostile is within `FIGHT_R` 45 m, and the threat within 160 m plus the newcomer must fit `budget(lv)` = 1 + 1.1·lv; nothing spawns for `GRACE` (20 s) after leaving a village, or while flying. A granted `mayspawn` starts the next gap (the caller spawns right away). Spawns come ahead or to a side of the player (60-95 m). Creature bites and robot blows are scaled by `FOE_HIT` 1.35. Gates: gnawers and brambles anywhere, ravagers from 1.2, leechwings 2.5, robots per `ROBOTS[k].min`, bandit patrols 2, ambushes 2.5, raiders 3; bandit camps stay ≥ 480 m from villages (`regionInfo`).
- Robots (`data/robots.ts`, `world/robots.ts`, the RD sheet; open world, unsaved; replaced the old field drones, which now only come from repair drones): RD-03 Scout Automaton (danger 1: fast blade packs, flank and slash), RD-01 Guardian Drone (1.8: spider legs, energy cannon, keeps 12–22 m, groups; more near ruins), RD-05 Repair Drone (3: hovers back, heals hurt robots with a beam, deploys up to 2 defence drones), RD-02 Sentinel (4.5: heavy biped, 3-bolt bursts and a crushing blow), RD-04 Artillery Walker (5: shells your position from 16–110 m, a red ring marks the blast; helpless up close), RD-06 Assault Construct (6: charges, hammer smash with knockback). Heavy ones take `armour` 0.55 of damage. Kills drop crystals and salvage (`loot`): Scrap Metal, Electronic Components (`circuit`), Power Core (`pcore`, rare); they count as 'drone' kills for bounties. Destroyed robots (`wreckRobot` → `updateScraps` in `world/robots.ts`, runtime only): shudder with sparks and flickering lights (`SHUDDER`), then give way (bipeds topple about their feet away from the hit, walkers' legs splay and the body drops, the repair drone falls out of the air), crash with a blast that tears an arm off heavy ones (`scene.attach`, thrown with gravity), smoke, go dark, lie `LIE_R` s, sink, `removeRobot`. Robots use `kind: 'robot'` in `foes()` / `damageFoe` → `hurtRobot`. Console: `spawn scout|guardian|repair|sentinel|artillery|assault`.
- Survival (`data/survival.ts` numbers, `world/survival.ts` runtime, HUD bars under HP): stamina (not saved; spent by sprinting, swimming, jumping and blade swings; regenerates after a short pause; at 0 you are exhausted until it is back to 30), calories and water. `char.kcal` is the body's energy store (max `KCAL.max` 3000, start 2400): it burns `KCAL.perDay` (2000) per game day at rest, times the activity (`G.activity`, set by the player's movement: walk 1.6, sprint/swim 4) and the carried weight (`BURN.perKg`), plus one-off costs (jump, blade swing, `burn()`). Below `KCAL.low` you are hungry (slower stamina regen), at 0 you starve (HP drain). Eating adds a food's `NOURISH[k].kcal` and puts its weight (`BULK`) into the stomach (`char.stomach`, `STOMACH.cap` 2 kg, digests 0.3 kg per game hour); `cantEat` refuses food when the stomach is full or the store is near max, so bulky low-calorie food (caps, pods) fills you before it feeds you. Water `char.water` 0..100 as before (drinks do not fill the stomach). Old saves: the 0..100 food bar becomes the same share of `KCAL.max`. The blade hits hard (`BLADE.dmg` 5 vs the Blaster's 1 per shot) but each swing costs stamina; exhausted, swings are slow (`tiredRate`, ignoring any speed bonus) and half as strong. Console: `eat`.
- Weight and bulk (`BULK` in `data/items.ts`: kg and litres per item; `PACK`): the backpack holds `PACK.vol` 40 L (a hard limit: `roomFor`, and the optional `cap` of `putItems` / `putSlot` / `moveStack` / `dropStack` in `inventory.ts`; every way into `char.inv` passes it, e.g. `addItem`, `packRoom`); weight is soft: above `PACK.comfy` (20 kg) you walk slower and sprint costs more, above `PACK.max` (35 kg) you are overloaded (slow, no sprint or jump). Equipped relics and fitted attachments weigh nothing. The backpack and container windows show `loadText()`, item details show kg, L and kcal.
- Dropped items lie on the ground as what they are (`world/pickmodels.ts` `lyingModel`: logs with cut ends and rings, planks, stones and ore lumps with veins, scrap (bent plate, cog, pipe), circuit boards, power cores, hides, fangs, incisors, plates, membranes, meat on the bone, caps, pods, crystals, bread, bowls, flasks, fire kits, tools, wire/rope coils, nails; trade goods as crates): `Pickup.rest`, no hover or spin, `restY` puts them on the terrain or the voxel floor. Other items keep the floating tokens (relics, keys, boxes).
- Food from the wilds: kills drop items besides crystals (`DROPS` in `data/creatures.ts`): only the big herbivore Bramble gives Raw Meat (+ Bramble Plate); Ravager (Hide, Fang), Leechwing (Membrane) and Gnawer (Incisor) give crafting materials (`type: 'mat'`, crafting later; Oskar buys them). Raw meat can be eaten (`RAW_SICK` chance to hurt) or roasted into Roasted Meat at any fire (E at a bandit camp's campfire or at a fire lit from a Fire Kit, `world/cooking.ts`, unsaved, `FIRE_LIFE`) or by Jan for `COOK_PRICE`. Jan and Oskar buy goods (`BUYS` in `data/npcs.ts`, the 'sell' option). Food pickups are lime (`FOOD_COLOR`), materials bone-white.
- Edible plants (RF-01 sheet): F-01 Nutrient Mushroom Cluster (shady forest) and F-02 Alien Fruit Pod Tree (forest edges, low ground) from `gen/flora.ts` (`chunkPlants`, at most one per chunk, cached; plants clear the trees around them via `plantsNear` in `gen/trees.ts`), F-03 Nutrient Crystals in dungeon room corners (`DungeonMap.crystals`, own RNG stream). `world/flora.ts` draws them (the picked part is a separate lime object) and handles E to pick; picked state is `char.harvest[key]` = game time, removed once regrown (`FORAGE` in `data/survival.ts`: caps 12 h, pods 24 h, crystals 48 h). Keys: `shroom|pod:<cx>:<cz>` (canonical chunk), `crys:<dungeonKey>:<i>`.
- Crafting (`data/crafting.ts` pure: `RECIPES`, `craft`, `GATHER`; `ui/craft.ts` window inside the dialogue panel): materials are Log, Stone, Scrap Metal (drones 35%, guardians 3, bandits 30% / leaders always, raider wreck trunks) plus the creature materials. Tools (`type: 'tool'`, carried in the backpack): Hatchet → hold E at a tree (`world/gather.ts`: `strike` starts the work, `updateGather` swings a blow every `GATHER.swing` 1.05 s while E is held, the tool drawn on the camera, weapons away via `working()` in the armed rule; 6 blows, big trees 14; blows kept while you stay; logs drop as pickups), Pickaxe → hold E at a rock with r ≥ `ROCK_MIN_R` (5 / 9 blows, stones). Ore veins: `Rock.ore` 'iron' | 'copper' (`oreOf` in `gen/trees.ts`, own hash stream per rock index; `ORE`: 5% of workable rocks in the lowlands, up to ~45% in the mountains, copper 35–50%), drawn as zigzag streaks and nuggets (`PropBatch.vein`, `ORE_COLOR` in `world/overworld.ts`), ×1.6 blows, drop Iron Ore / Copper Ore lumps (`ironO`, `copperO`) besides the stones, regrow in `GATHER.ore.regrow` 4 days (the harvest time is set ahead). Ore recipes: forge smelts iron ore + log → scrap, iron ore → wire, copper ore + scrap → electronic components; a bench packs 8 lumps + 2 planks into a market crate (`ore` / `copper` goods); Oskar buys the lumps. Crafting takes time (`Recipe.time`, `craftTime`, `CRAFT_TIME` bench 3 s / forge 6 s; planks 5 s): `ui/craft.ts` runs a job with a progress bar (Craft / ×5, Stop; materials used only when a batch completes, leaving or closing the window stops it). E ignores key repeat (`input.ts`). Felled trees / broken rocks are `char.harvest` keys `tree|rock:<cx>:<cz>:<i>` (index in `chunkTrees` / `chunkRocks`, canonical cx; `gatherKey` maps loaded objects to keys), a stump is drawn, the chunk is rebuilt (`rebuildChunkAt`), they regrow after 3 / 2 game days. Stations: every village blacksmith's workbench is a 'forge' (dialogue option 'craft', all recipes); a Workbench Kit sets up a 'bench' in the wilds (`world/benches.ts`, `char.benches`, max 3, E to use, can be packed up again) for the plain recipes (fire kit, tools, flask, medkit, workbench kit). Oskar sells Hatchet/Pickaxe and buys logs, stone, scrap.
- Noise (`world/noise.ts`): `makeNoise(at, radius)` for loud things (Blaster shot = gun stat `noise`, 55 m; Suppressor barrel attachment cuts it to ~12 m; vehicle cannon 95 m; EMP 35 m), `onNoise` listeners react: ground creatures (not Brambles) within the radius go to `investigate` the spot (and hunt you if they see you there), Leechwings hear from 1.8× as far, bandits (not ambushers) within 0.8× are alerted, drones within 0.5× give chase. Noise also builds `heat` in world/creatures.ts: more and faster spawns, placed around the noise and sent to investigate it; heat fades over ~25 s. Leechwing flight is steered (turn-rate-limited arcs, banking): soaring loops of 40–55 m radius at ~24 m, spotting prey up to 90 m (with line of sight), stalking in tightening, descending circles, diving, then a long climbing pull-out; with no player about they hunt Gnawers.
- Figures and arms (`world/rig.ts`): every human figure (`makeFigure(color, kit, shield, big)` in `world/npc.ts`) has two-segment arms solved by a two-bone IK each frame (`poseRig(rig, {swing, aim, strike, recoil})`) so the hands sit on the weapon; kits: 'none' (arms swing with the walk), 'rifle' (both hands, low ready → shouldered with `aim`, recoil kick; the boss's is longer with a scope), 'pistol' (one hand, raised to aim), 'sword' (guard, then a stroke by `strike` 0..1: wind-up over the shoulder, slash, recover; optional shield on the left arm), 'spear' (guards: upright, shield), 'drive' (raider crews: hands on the wheel / cannon). `muzzleLocal(rig)` gives the muzzle for bolts. Bandits pick a kit in `kitOf` (gunners rifle or pistol 35%, bruisers sword + shield 50%, the boss a scoped rifle); a bruiser's blow lands mid-slash (`Bandit.blow`), a shield halves damage from the front (`hurtBandit`). Death (`fallBandit(b, from)` in `world/bandits.ts`, also from caravan guns): out of `W.bandits` at once; a `Corpse` (runtime only) animates: weapon and shield `scene.attach`ed and thrown with gravity and spin (one bounce, then rest on `groundUnder`), knees buckle, the figure topples about its feet away from the blow (`lie` angle from the ground slope at the head), bounces, colour drains, lies `LIE` s, sinks over `SINK` s, then `removeBandit`.
- Bandits (`world/bandits.ts`, human enemies built from the NPC figure, amber; the Bandit Boss is red): Gunners keep 12–20 m, strafe and fire bursts of visible, dodgeable bolts; Bruisers rush with blades; each camp has one Boss. They need line of sight, alert their whole group, sometimes flee when hurt. Bolts that hit a closed-cab vehicle damage its engine/wheels instead of the driver. Camps are a POI type (`camp`, `gen/camps.ts`: crates/barricades as voxels for cover, A-frame tents, campfire, stash container `camp:<id>`), placed per region away from the village and from ruins; a cleared camp stays empty for 30 min (`char.camps`). Small patrols roam further out. Console: `spawn bandits`.
- Raiders and ambushes (`world/raiders.ts`, open world, danger ≥ 1 / 0.8): raider vehicles are AI-driven vehicles (`spawnAIVehicle`, `steerVehicle` with obstacle feelers; tinted amber, not claimable while `ai`) with a driver and a roof gunner; they chase from behind, circle and fire bursts, ram players on foot. Shot to pieces they become claimable wrecks (damaged parts, 50% cannon kept, some trunk loot) and the crew bails out as normal bandits. Ambushes spawn on roads ahead of the player: a roadblock of 3 barricades (`placeRoadblock`, `Barricade` with `BARRICADE_HP` 14; `ambushHit` obstacles for players and vehicles; `rayBarrier` / `hurtBarrier` in `shoot()` and `fireCannon()`, so shots wear them down; they stay after the bandits die and go when all are shot apart or you are 230 m away; `barriersNear`) and bandits lying low (`sight` 18) on both sides. Console: `spawn raider [mastodon]`, `ambush`.
- Quests (`gen/quests.ts` pure generator, `world/quests.ts` progress, `ui/board.ts` window): every village has a notice board on its plaza (`VillageMap.board`, `boardDeco`); each shows 4 notices. Gridholm's board is `char.board` (seq numbering and ids `q<n>` as ever), the others `char.boards[villageId]` (`boardOf`, made when first read; `refreshBoard` takes the old offers down on all of them and they refill when read). `generateQuest(t, n, taken, town, camps)` takes a `QuestTown` (`questTown(poi)`: position, `pay` = `boardPay(ringDanger(...))`, resident names from the loaded NPCs; `GRIDHOLM_TOWN` by default): other boards use their own RNG stream (`hash(world, id, n)`), ids `q<id>.<n>`, measure hunts/ruins/wrecks/camps from the village, drop the dealer's errand, and stamp `town`, `townName`, `giverName` on the quest. Claims only at the posting board (`claim(id, poi)`, `townOf`, `boardName`), errands only with that village's resident (`questOptions(role, town)` / `questTalk(id, town)`, `giverOf`); `questTarget` points back to the village once done. A camp is not posted twice on one board. Up to 3 accepted (`char.quests`). Kinds: bounty (kill N of a kind anywhere, incl. bandits), camp (clear a real bandit camp), hunt (a group + leader — Pack Alpha / Old Bull / Matriarch — at a real spot; `describeSpot` derives the wording and the creature from the terrain there), fetch (a resident errand: talk to the giver, then an item in a real ruin's dungeon — first guardian's room of depth 1 sector 0,0 — or by a real abandoned vehicle's parking spot). Board quests are claimed at the board, errands at the giver. Below the notices a Deliveries section (`deliveries()` in `ui/board.ts`): your contracts with Drop (`dropContract`) and `boardContracts(poi)` from `ui/contracts.ts` (the village's own offers plus the 4 nearest supply orders of villages within `CONTRACT.far`), taken with `acceptOffer` (the same path as the store's 'contracts' option; a haul still has to be loaded at its village). HUD tracker `#qtrack`, gold markers on the maps.
- Containers: chests roll their contents once on first open and keep what the player leaves or puts in (`char.containers`, key `chest:<dungeonKey>:<index>`). Chests, stashes and trunks use the transfer window (`ui/transfer.ts`): container grid on top, backpack below, two-way.
- Item slots (`ui/slots.ts`): every inventory window renders slots with `slotHTML` and gets drag and drop (pointer events: mouse and touch) from `bindSlots`; a press without movement is a click (quick action). Stack moves are pure functions in `inventory.ts` (`dropStack` merges or swaps, `putSlot`). `Slot.c` is the condition of a worn part (tires keep their wear off the vehicle); such items never stack.
- Guns (`data/weapons.ts`, pure): `GunSpec` base stats (damage, interval, range, magazine, reload, zoom) and attachment slots (optic, barrel, magazine); `gunStats` applies the fitted attachments (`char.gunMods`, fitted in the backpack). `world/weapons.ts`: magazine + reload (R / RLD, automatic when empty; reserve ammo unlimited for now), aiming zoom (right mouse / AIM), scope overlay at 3×+, attachments shown on the held gun. Oskar the blacksmith sells attachments (`ATTACH_PRICE`).
- Vehicle service (`ui/service.ts`): slots for each wheel's tire, the engine (drop Engine Parts to repair), 2 engine upgrade slots (`ENGINE_UPGRADES`: Turbocharger, Engine Guard; `engineBoost`, `hurtEngine`), the roof mount (cannon) and the hull (drop Hull Plating), plus the backpack. Kuba buys used parts back scaled by their condition.
- Compass and map boards: the Compass (`compass`, a 'tool'; Zofia's store sells it, bench recipe scrap + circuit) shows `ui/compass.ts` (canvas strip at the top, only on the surface while carried): heading, the nearest village more than 150 m away and quest markers (`bearingTo`, `point8`, `fmtDist`). Every village has a map board (`VillageMap.mapBoard`, drawn by `mapBoardDeco` in `world/overworld.ts`); E opens `ui/areamap.ts` `openAreaMap`: 900 m around the village (terrain, lakes, roads, POIs, the 14 nearest named), edge arrows to the 3 nearest other villages, a list with distance/direction; reading it `discover`s the POI chunks.
- **Building outside the villages is closed for now** (`BASES_OPEN = false` in `data/building.ts`): no new flag (`useItem` flagpole), no build list (B), no Workbench Kit set up in the wilds (its recipe is hidden), `stockFor` leaves out `BASE_ITEMS` (flagpole, turret kit, code lock); existing claims, parts, doors, locks and turrets keep working. The direction instead: the hero helps fortify villages (better walls etc.) and has a house in town; other players can live in the same town or pick another. The claim/building code below stays for reuse.
- Land claims (the first step towards player bases): a Flagpole (`flagpole`, Zofia sells it; later a workbench recipe) is used from the backpack → placing mode in `world/claims.ts` (`startPlacing`, `updatePlacing` from the main loop instead of `attack`; click/E confirms, right mouse/Esc cancels): a hologram (depthTest off) with the pole, the levelled floor grid, cut/fill posts, the blend spokes and the dashed claim border; red with the reason from `claimProblem` (pure, `gen/claims.ts`: villages 150 m, places, roads, water, steep ground `MAX_CUT`, the ice, other claims). `char.claims` `{x, z, y, t}` (y = `padHeight`, mean ground to 0.5 m). The terrain takes them via `Terrain.setClaims` (clears its caches; `Features.claims`, `claimFlatten` after pads, before lakes): flat `CLAIM.flat` 12 m, blend 12 m (`CLEAR_R`), claim radius `CLAIM.r` 30 m. `buildChunk` leaves out trees, rocks and plants within `CLEAR_R` (`Terrain.claimAt`); `rebuildChunksNear` redraws the ground. E at the flag (`nearFlag`) takes it down. HUD "Your claim", compass and map markers. Not a safe zone. Next: building parts on the claim, owner-only doors/dismantling, raiding with explosives, turrets.
- Hands, back and wear (`char.hands` 1 slot, `char.back` 2 slots, `char.wear` per `WearSlot`): weapons are items (`blaster`, `blade`, type 'weapon', `WEAPON_KIND` → `G.weapon` 0/1, -1 when the hands are empty or full); `world/weapons.ts` `syncHeld` (hooked via `onHandsChanged`; call `handsChanged()` after touching `char.hands`), `drawBack(i)` (keys 1/2), `swapWeapon` (Q/wheel), `holster` (X). `HANDS_ONLY` items (wheels, cannon, flagpole, workbench kit) never enter the backpack (`roomFor` gives 0 under a cap); `addItem` puts them in the hands (the held weapon is stowed by `stowHeld`: back, else backpack), `takeOne`/`hasItem` also look in the hands, crafting delivers them to the hands (`craft(..., hands)`). Slot ids in windows: `h:0` hands, `k:i` back, `g:i` wear (`WEAR_SLOTS`), `listOf(w)` in character.ts; the backpack, transfer and service windows show them. Armour: `WEAR[k].def`, combined in `G.S.def`, applied by `armoured(dmg)` at every combat hit on the player. `carriedKg()` = backpack + hands + back + wear (the load). Oskar sells weapons and gear (`GEAR_PRICE`) and buys them back. Old saves get the Blaster in hand and the Blade on the back (`newChar` defaults).
- Building (on a claim): `data/building.ts` `PIECES` (wallW/doorW/roofW, wallM/doorM/roofM: `needs` materials used up, `tools` that stay in the backpack, `cut` = the tool to take it down) and `BUILD` (2 m cells, walls 2.8 m, doors 1.1 x 2.2 m). `gen/base.ts` (pure, claim-local u/v metres round the flag): `Part {k, gx, gz, d, open?}` (walls/doors on cell edges, d 0 = along u, 1 = along v; roofs on cells), `snap`, `partProblem` (within `CLAIM.flat`, not through the flagpole, one part per slot via `slotKey`), `solidSegs` / `baseHitLocal` (walls, door jambs, shut door leaves), `refund` (half). Saved as `char.claims[i].parts`. `world/building.ts`: PropBatch models per claim (planks/seams lines), `baseHit` in `G.obstacle` and in the creature/robot/bandit `forbidden` hook, doors (`nearDoor`, `toggleDoor`, E), build mode (`startBuilding` from the B list `ui/build.ts`, `updateBuilding` in the main loop like flag placing: hologram, click = `placePart`, F = `dismantle`, right mouse / Esc = `stopBuilding`). Tools: hammer, saw, screwdriver, pliers, welder, torch, shovel (Oskar, `TOOL_PRICE`); supplies nails, rope, wire, code locks (Zofia, `SUPPLY_PRICE`); planks from logs with a saw (`Recipe.tools`).
- Storeys and stairs: `Part.lv` (storey; old roofs default to 1, `lvOf`), `BUILD.storey` 3 m (2.8 m wall + 0.2 m slab), `floorH(lv)`; 'roof' = floor slab at the top of storey lv-1; stairs (`d` 0..3 = +u,+v,-u,-v, two cells, `cellsOf`, `stairDir` from the yaw) climb one storey. Support rules in `partProblem`; `dismantle` refuses parts something above rests on. Physics hooks set in `openWorld`: `G.floor` = `baseFloor` (slab tops and stair ramps within `STEP_UP`; `moveAxis` skips the slope limit for them, `settleOnGround` lands on floors you were above), `G.rayBlock` = `baseRay` (in `rayWorld`: every shot and every enemy sight line; bolts also check their step so they cannot skip thin walls), `G.solid` = `baseSolid` (in `emptyAt`). Slab boxes only stop you from below (`Box.slab`). Boxes cached per claim, invalidated by `redraw` (`rev`).
- Code locks (`codelock` item, `Part.lock {code, on, auth[]}`, `char.pid`): L at a door (`lockMenu`) fits one (keypad `ui/keypad.ts`, 4 digits), changes the code (auth reset), locks/unlocks, takes it off; E at a locked door you are not authorised for asks for the code (`toggleDoor`). Locked doors cannot be taken down without the code. Not yet: explosives/raiding, floors on the ground storey, windows.
- Auto turrets: part `turret` (shape 'turret', one cell, shares the cell slot with stairs; on the pad or on a slab of its storey; needs `turretkit` (Oskar, `TOOL_PRICE`) + 2 wire, screwdriver + pliers; `PieceSpec.refund` gives the kit back; solid plinth box). `world/turrets.ts` (runtime, unsaved): `syncTurrets` keeps a turning head per turret near the player, `updateTurrets` (main loop, while playing) picks the nearest hostile `foes()` entry (not calm Brambles) within `TURRET.range` with `rayWorld` line of sight, turns at `TURRET.turn`, fires hitscan via `damageFoe` every `TURRET.rate` s with `makeNoise`. `Part.off` = switched off (E, `nearTurret` / `toggleTurret`). Not yet: turret health, ammo or power, targeting settings.
- `data/` — items, NPC texts, vehicles. `save.ts` — persistence and version migrations.
